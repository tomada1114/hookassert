{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if [ \"$OK\" = \"true\" ]; then exit 0; else exit 2; fi"
          }
        ]
      }
    ]
  }
}
