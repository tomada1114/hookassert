{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if [ \"$RISK\" = \"high\" ]; then echo blocked; exit 2; fi"
          }
        ]
      }
    ]
  }
}
